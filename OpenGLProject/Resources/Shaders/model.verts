#version 330 core

uniform mat4 mvp;

layout (location = 0) in vec3 position;
layout (location = 2) in vec2 uv;

out vec2 fragUV;

void main()
{
	gl_Position = mvp * vec4(position, 1);
	fragUV = uv;
}
