#version 330 core

uniform float currentTime;

in vec3 fragNormal;

out vec4 color;

void main()
{
	vec3 col = vec3(0.5f * (1 + sin(currentTime)), 0.5f * (1 + cos(currentTime)), 0);
	color = vec4(fragNormal + col, 1);
}
